- optimize R^2 with different regime clusters
- matplotlib figures
- encode KMeans regime values in the dataset (before feeding to LSTM). Don't apply a scaler on the encoded column
- rebuild the dataset without covid years and rerun the code
- predicting raw price levels vs returns (?)